#lang racket

;Dominio:
;Recorrido
(define (system string)
  list("#"))

;(define run (system funcion)
 ; )

;(define (add-drive system char string int)
  
 ; )

(define (add ruta-inicial string)
  string-join ruta-inicial string "/")